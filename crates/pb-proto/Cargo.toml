[package]
name = "pb-proto"
version = "0.1.0"
description = "Shared wire types and identifiers. Single source of truth for every client and service."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
